#!/bin/bash

. tools/ci/common.sh
set -eo pipefail

./docker.sh exec "set -x; ./scion.sh topology -sd=go -ps=go ${DOCKER:+zkclean -d}"

# Run tests inside the CI container.
./docker.sh exec "set -eo pipefail; ./integration/integration_test.sh |& tee logs/integration.run"
