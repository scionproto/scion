- label: Prepare scion_base and scion images
  command:
      - ./tools/ci/prepare_image 7fcd650ae3adb42b5bad5592ae4ec7d2107693c8174c0dc92c6e535ef3d74e82
      - ./docker.sh build
  concurrency: 1
  concurrency_group: "build-scion"
# FIXME(worxli): we should commit the built code and push the image, then use that image for the
# rest of the pipeline
- wait
- label: Start container
  command:
      - mkdir $SCION_MOUNT
      - ./docker.sh start
- wait
