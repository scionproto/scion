Within AD addressing:
224.224.0.0/16 - reserved for SCION-related multicast (TODO test if src in ip
layer can be multicast)
10.224.0.0/16 - reserved for SCION-related uni/anycast 

    TYPE                SRC                 DST                         MSG
BEACON *        10.224.0.1 (ANYCAST)    UNICAST                         BS->ER
BEACON **       10.224.0.1 (ANYCAST)    224.224.0.1 (MULTICAST)         ER->BS
CERT_REQ        UNICAST                 10.224.0.2 (ANYCAST)            CS->CCS
CERT_REP        10.224.0.2 (ANYCAST)    UNICAST                         CCS->CS
#PATH_REQ_LOCAL  UNICAST                 10.224.0.3 (ANYCAST)            EH->PS
#PATH_REP_LOCAL  10.224.0.3 (ANYCAST)    UNICAST                         PS->EH
PATH_REQ        UNICAST                 10.224.0.4 (ANYCAST)            PS->CPS
PATH_REC        10.224.0.4 (ANYCAST)    UNICAST                         CPS->PS
#PATH_REP        10.224.0.4 (ANYCAST)    UNICAST                         CPS->PS
#PATH_REQ_TDC    UNICAST                 10.224.0.5 (ANYCAST)            CPS->CPS
#PATH_REP_TDC    10.224.0.5 (ANYCAST)    UNICAST                         CPS->CPS
#UP_PATH         UNICAST                 224.224.0.2 (MULTICAST)         BC->PS
#UP_PATH         224.224.0.2 (MULTICAST) UNICAST                         PS->EH
#PATH_REG        UNICAST                 224.224.0.3 (MULTICAST)         BC->CPS
ROT_REQ_LOCAL   UNICAST                 10.224.0.6 (ANYCAST)            BS->CS
ROT_REP_LOCAL   10.224.0.6 (ANYCAST)    UNICAST                         CS->BS
OFG_KEY_REQ     UNICAST                 10.224.0.7 (ANYCAST)            ER->CS*
OFG_KEY_REP     10.224.0.7 (ANYCAST)    UNICAST                         CS->ER*
IFID_REQ ***    UNICAST                 10.224.0.10(UNICAST)            ER->ER
IFID_REP ***    10.224.0.10 (UNICAST)   UNICAST                         ER->ER
ROT_REQ         UNICAST                 10.224.0.8 (ANYCAST)            CS->CCS
ROT_REP         10.224.0.8 (ANYCAST)    UNICAST                         CCS->CS
CERT_REQ_LOCAL  UNICAST                 10.224.0.9 (ANYCAST)            BS->CS
CERT_REP_LOCAL  10.224.0.9 (ANYCAST)    UNICAST                         CS->BS
DATA            UNICAST                 UNICAST                         ER->EH 
DATA            UNICAST                 UNICAST                         EH->ER 

#   unused, replaced by PATH_REC or PATH_REQ
*   not sure (also other BSes may be interested...)
**  ANY or UNI?? have to look into BS’s processing code. Leave it as 1st
BEACON type and address it in lower layer.
*** should be special fixed, unicast src here, otherwise there is
problem between that req/rep and standard forwarding. Solution: special unicast
addr in SCION layer, while peer addr in IP

